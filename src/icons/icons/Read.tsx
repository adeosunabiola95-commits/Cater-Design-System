/** @generated — Remix Icon (RiBookReadLine) */
import { RiBookReadLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Read = createRemixIcon(RiBookReadLine, 'Read');
