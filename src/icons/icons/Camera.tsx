/** @generated — Remix Icon (RiCameraLine) */
import { RiCameraLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Camera = createRemixIcon(RiCameraLine, 'Camera');
