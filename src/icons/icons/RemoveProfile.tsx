/** @generated — Remix Icon (RiUserMinusLine) */
import { RiUserMinusLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const RemoveProfile = createRemixIcon(RiUserMinusLine, 'RemoveProfile');
