/** @generated — Remix Icon (RiDiscLine) */
import { RiDiscLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Disk = createRemixIcon(RiDiscLine, 'Disk');
