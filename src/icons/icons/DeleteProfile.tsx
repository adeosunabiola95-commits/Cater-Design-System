/** @generated — Remix Icon (RiUserUnfollowLine) */
import { RiUserUnfollowLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const DeleteProfile = createRemixIcon(RiUserUnfollowLine, 'DeleteProfile');
