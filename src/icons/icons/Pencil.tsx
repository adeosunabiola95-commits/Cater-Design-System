/** @generated — Remix Icon (RiPencilLine) */
import { RiPencilLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Pencil = createRemixIcon(RiPencilLine, 'Pencil');
