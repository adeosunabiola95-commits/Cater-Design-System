/** @generated — Remix Icon (RiPlantLine) */
import { RiPlantLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Tomato = createRemixIcon(RiPlantLine, 'Tomato');
