/** @generated — Remix Icon (RiFilterLine) */
import { RiFilterLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Filter = createRemixIcon(RiFilterLine, 'Filter');
