/** @generated — Remix Icon (RiSlideshowLine) */
import { RiSlideshowLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Slider = createRemixIcon(RiSlideshowLine, 'Slider');
