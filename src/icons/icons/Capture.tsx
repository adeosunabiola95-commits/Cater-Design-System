/** @generated — Remix Icon (RiCameraLensLine) */
import { RiCameraLensLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Capture = createRemixIcon(RiCameraLensLine, 'Capture');
