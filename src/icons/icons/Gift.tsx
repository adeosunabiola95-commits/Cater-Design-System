/** @generated — Remix Icon (RiGiftLine) */
import { RiGiftLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Gift = createRemixIcon(RiGiftLine, 'Gift');
