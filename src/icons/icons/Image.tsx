/** @generated — Remix Icon (RiImageLine) */
import { RiImageLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Image = createRemixIcon(RiImageLine, 'Image');
