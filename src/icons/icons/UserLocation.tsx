/** @generated — Remix Icon (RiUserLocationLine) */
import { RiUserLocationLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const UserLocation = createRemixIcon(RiUserLocationLine, 'UserLocation');
