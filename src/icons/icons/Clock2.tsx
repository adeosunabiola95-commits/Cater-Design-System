/** @generated — Remix Icon (RiTimerLine) */
import { RiTimerLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Clock2 = createRemixIcon(RiTimerLine, 'Clock2');
