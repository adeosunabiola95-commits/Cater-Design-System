/** @generated — Remix Icon (RiStoreLine) */
import { RiStoreLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Store = createRemixIcon(RiStoreLine, 'Store');
