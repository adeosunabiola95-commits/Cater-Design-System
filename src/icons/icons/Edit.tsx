/** @generated — Remix Icon (RiEditLine) */
import { RiEditLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Edit = createRemixIcon(RiEditLine, 'Edit');
