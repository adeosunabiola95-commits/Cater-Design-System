/** @generated — Remix Icon (RiMailLine) */
import { RiMailLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Mail = createRemixIcon(RiMailLine, 'Mail');
