/** @generated — Remix Icon (RiDeleteBinLine) */
import { RiDeleteBinLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Delete = createRemixIcon(RiDeleteBinLine, 'Delete');
