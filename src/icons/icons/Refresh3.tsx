/** @generated — Remix Icon (RiLoopRightLine) */
import { RiLoopRightLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Refresh3 = createRemixIcon(RiLoopRightLine, 'Refresh3');
