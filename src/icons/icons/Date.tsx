/** @generated — Remix Icon (RiCalendarLine) */
import { RiCalendarLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Date = createRemixIcon(RiCalendarLine, 'Date');
