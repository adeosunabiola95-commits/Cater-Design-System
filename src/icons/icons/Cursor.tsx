/** @generated — Remix Icon (RiCursorLine) */
import { RiCursorLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Cursor = createRemixIcon(RiCursorLine, 'Cursor');
