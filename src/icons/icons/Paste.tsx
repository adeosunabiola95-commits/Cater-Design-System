/** @generated — Remix Icon (RiClipboardLine) */
import { RiClipboardLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Paste = createRemixIcon(RiClipboardLine, 'Paste');
