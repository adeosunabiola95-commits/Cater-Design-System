/** @generated — Remix Icon (RiDiscussLine) */
import { RiDiscussLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const GroupChat = createRemixIcon(RiDiscussLine, 'GroupChat');
