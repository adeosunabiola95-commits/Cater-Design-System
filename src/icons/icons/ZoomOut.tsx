/** @generated — Remix Icon (RiZoomOutLine) */
import { RiZoomOutLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const ZoomOut = createRemixIcon(RiZoomOutLine, 'ZoomOut');
