/** @generated — Remix Icon (RiSearchLine) */
import { RiSearchLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Search = createRemixIcon(RiSearchLine, 'Search');
