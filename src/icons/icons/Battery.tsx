/** @generated — Remix Icon (RiBatteryLine) */
import { RiBatteryLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Battery = createRemixIcon(RiBatteryLine, 'Battery');
