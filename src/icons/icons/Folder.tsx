/** @generated — Remix Icon (RiFolderLine) */
import { RiFolderLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Folder = createRemixIcon(RiFolderLine, 'Folder');
