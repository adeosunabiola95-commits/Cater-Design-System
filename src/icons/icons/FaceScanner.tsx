/** @generated — Remix Icon (RiScanLine) */
import { RiScanLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const FaceScanner = createRemixIcon(RiScanLine, 'FaceScanner');
