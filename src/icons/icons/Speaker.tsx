/** @generated — Remix Icon (RiSpeakerLine) */
import { RiSpeakerLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Speaker = createRemixIcon(RiSpeakerLine, 'Speaker');
