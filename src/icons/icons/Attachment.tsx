/** @generated — Remix Icon (RiAttachmentLine) */
import { RiAttachmentLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Attachment = createRemixIcon(RiAttachmentLine, 'Attachment');
