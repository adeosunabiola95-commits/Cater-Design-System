/** @generated — Remix Icon (RiDownloadLine) */
import { RiDownloadLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Download = createRemixIcon(RiDownloadLine, 'Download');
