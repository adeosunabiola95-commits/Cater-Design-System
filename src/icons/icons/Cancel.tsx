/** @generated — Remix Icon (RiCloseLine) */
import { RiCloseLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Cancel = createRemixIcon(RiCloseLine, 'Cancel');
