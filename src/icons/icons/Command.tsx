/** @generated — Remix Icon (RiCommandLine) */
import { RiCommandLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Command = createRemixIcon(RiCommandLine, 'Command');
