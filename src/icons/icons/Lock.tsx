/** @generated — Remix Icon (RiLockLine) */
import { RiLockLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Lock = createRemixIcon(RiLockLine, 'Lock');
