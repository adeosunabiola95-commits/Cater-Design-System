/** @generated — Remix Icon (RiAddLine) */
import { RiAddLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Plus = createRemixIcon(RiAddLine, 'Plus');
