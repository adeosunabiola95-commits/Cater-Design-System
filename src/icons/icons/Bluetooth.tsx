/** @generated — Remix Icon (RiBluetoothLine) */
import { RiBluetoothLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Bluetooth = createRemixIcon(RiBluetoothLine, 'Bluetooth');
