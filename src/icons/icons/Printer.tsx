/** @generated — Remix Icon (RiPrinterLine) */
import { RiPrinterLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Printer = createRemixIcon(RiPrinterLine, 'Printer');
