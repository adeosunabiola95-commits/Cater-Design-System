/** @generated — Remix Icon (RiStackLine) */
import { RiStackLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Layer = createRemixIcon(RiStackLine, 'Layer');
