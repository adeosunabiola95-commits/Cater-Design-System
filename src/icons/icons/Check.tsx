/** @generated — Remix Icon (RiCheckLine) */
import { RiCheckLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Check = createRemixIcon(RiCheckLine, 'Check');
