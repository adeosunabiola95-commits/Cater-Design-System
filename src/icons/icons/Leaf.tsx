/** @generated — Remix Icon (RiLeafLine) */
import { RiLeafLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Leaf = createRemixIcon(RiLeafLine, 'Leaf');
