/** @generated — Remix Icon (RiLockUnlockLine) */
import { RiLockUnlockLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Unlock = createRemixIcon(RiLockUnlockLine, 'Unlock');
