/** @generated — Remix Icon (RiBallPenLine) */
import { RiBallPenLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Pencil4 = createRemixIcon(RiBallPenLine, 'Pencil4');
