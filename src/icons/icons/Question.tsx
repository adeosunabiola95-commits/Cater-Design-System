/** @generated — Remix Icon (RiQuestionLine) */
import { RiQuestionLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Question = createRemixIcon(RiQuestionLine, 'Question');
