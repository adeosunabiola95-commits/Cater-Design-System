/** @generated — Remix Icon (RiNotificationLine) */
import { RiNotificationLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Notication = createRemixIcon(RiNotificationLine, 'Notication');
