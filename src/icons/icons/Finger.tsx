/** @generated — Remix Icon (RiFingerprintLine) */
import { RiFingerprintLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Finger = createRemixIcon(RiFingerprintLine, 'Finger');
