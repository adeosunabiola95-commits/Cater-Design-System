/** @generated — Remix Icon (RiMenuLine) */
import { RiMenuLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const HamburgerMenu = createRemixIcon(RiMenuLine, 'HamburgerMenu');
