/** @generated — Remix Icon (RiServerLine) */
import { RiServerLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Server = createRemixIcon(RiServerLine, 'Server');
