/** @generated — Remix Icon (RiTimeLine) */
import { RiTimeLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const Clock = createRemixIcon(RiTimeLine, 'Clock');
