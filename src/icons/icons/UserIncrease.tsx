/** @generated — Remix Icon (RiUserAddLine) */
import { RiUserAddLine } from '@remixicon/react';
import { createRemixIcon } from '../createRemixIcon';

export const UserIncrease = createRemixIcon(RiUserAddLine, 'UserIncrease');
