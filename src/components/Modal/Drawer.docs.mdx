{/* Drawer.docs.mdx */}
import { Meta, Canvas, ArgTypes } from '@storybook/addon-docs/blocks';
import { Drawer } from './Modal';
import * as DrawerStories from './Drawer.stories';

<Meta of={DrawerStories} />

# Drawer (side panel)

Slides in from the **left** or **right** for secondary flows. Implemented in `Modal.tsx` next to `Modal`.

<Canvas of={DrawerStories.AddHeaderText} sourceState="shown" />

## Props

<ArgTypes of={Drawer} />
